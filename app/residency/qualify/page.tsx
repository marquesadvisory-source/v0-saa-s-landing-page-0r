"use client"
import { useState } from "react"
import Link from "next/link"
import { ArrowRight, Check } from "lucide-react"
import { PageIntro, SiteFrame } from "@/components/editorial-shell"
import { EnquiryForm } from "@/components/enquiry-form"
const questions = [{ title: "What brings you to Costa Rica?", options: ["A new home base", "A future retirement", "A business or investment", "I am still exploring"] }, { title: "Where are you in the process?", options: ["Just researching", "Comparing pathways", "Ready to speak with counsel", "I have already started"] }]
export default function QualifyPage() { const [step, setStep] = useState(0); const [done, setDone] = useState(false); return <SiteFrame><PageIntro eyebrow="Residency qualification" title="Find your most useful next step."><p>A lightweight starting point—not legal advice or a determination of eligibility.</p></PageIntro><main className="mx-auto max-w-3xl px-5 py-16 md:px-8 md:py-24">{done ? <div className="bg-secondary p-7 md:p-10"><div className="mb-8 flex items-center gap-3"><Check className="text-accent" /><h2 className="font-serif text-3xl text-primary">Let us add context.</h2></div><EnquiryForm type="residency-qualification" /></div> : <div className="border border-border p-7 md:p-10"><p className="text-[10px] font-bold uppercase tracking-[0.18em] text-accent">Question {step + 1} of {questions.length}</p><h2 className="mt-5 font-serif text-4xl leading-tight text-primary">{questions[step].title}</h2><div className="mt-8 grid gap-3">{questions[step].options.map((option) => <button key={option} onClick={() => step + 1 < questions.length ? setStep(step + 1) : setDone(true)} className="flex items-center justify-between border border-border px-5 py-4 text-left text-sm text-primary hover:border-accent hover:bg-secondary">{option}<ArrowRight size={16} className="text-accent" /></button>)}</div><Link href="/residency" className="mt-8 inline-block text-xs text-muted-foreground underline">Skip and explore all pathways</Link></div>}</main></SiteFrame> }

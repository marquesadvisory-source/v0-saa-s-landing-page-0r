import { Mail, Phone } from "lucide-react"
import { PageIntro, SiteFrame } from "@/components/editorial-shell"
import { EnquiryForm } from "@/components/enquiry-form"
import { siteConfig } from "@/lib/site"
export default function ContactPage() { return <SiteFrame><PageIntro eyebrow="Contact" title="Start with a thoughtful conversation."><p>Whether you are exploring residency, a real asset opportunity or a capital mandate, we begin by understanding the context.</p></PageIntro><main className="mx-auto grid max-w-7xl gap-14 px-5 py-16 md:grid-cols-[0.8fr_1.2fr] md:px-8 md:py-24"><div><h2 className="font-serif text-4xl text-primary">Tell us where you are headed.</h2><p className="mt-5 text-sm leading-7 text-muted-foreground">A few details help us route your enquiry to the right advisory track.</p><div className="mt-10 grid gap-5 border-t border-border pt-6 text-sm text-primary"><a href={`mailto:${siteConfig.email}`} className="flex items-center gap-3 hover:text-accent"><Mail size={17} className="text-accent" />{siteConfig.email}</a><a href={`tel:${siteConfig.phone}`} className="flex items-center gap-3 hover:text-accent"><Phone size={17} className="text-accent" />{siteConfig.phone}</a></div></div><div className="bg-secondary p-6 md:p-10"><EnquiryForm /></div></main></SiteFrame> }
